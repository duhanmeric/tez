import React, { FC } from "react";
import { Button } from "@chakra-ui/react";

type SbButtonProps = {};

const SbButton: FC<SbButtonProps> = (props) => {
  return <Button>SbButton</Button>;
};

export default SbButton;
